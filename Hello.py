print("こんにちは")
# Pythonでは、値を出力させる場合にprint()を使う。数値ではなく文字列の場合は""か''で囲む。
# Rubyではputs 値で表現しているのに似ている。
print("today is..." "2026/3/26")
# 日付をyyyy/mm/ddで表現したい場合には、""か''で囲むことで文字列化する必要がある。
# これは/が演算子なので、文字列化しないまま入力すると普通に計算されてしまうため。
